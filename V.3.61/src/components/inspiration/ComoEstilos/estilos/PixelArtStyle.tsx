import { useId } from 'react';
import type { ComoEstiloProps } from '../types';
import { darken, lighten } from '../estiloColorUtils';

/* ═══════════════════════════════════════
   ESTILO: PIXEL ART
   Retro, cuadrícula, low-res, 8-bit
   ═══════════════════════════════════════ */
export function PixelArtStyle({ color = '#818cf8', className = '' }: ComoEstiloProps) {
  const id = useId().replace(/:/g, '');

  const c1 = color;
  const c2 = darken(color, 60);
  const c3 = darken(color, 110);
  const c4 = lighten(color, 70);
  const c5 = lighten(color, 110);

  // Tamaño del píxel
  const P = 8;

  // Helper para dibujar un píxel
  const px = (x: number, y: number, c: string, op: number = 1) => (
    <rect x={x * P} y={y * P} width={P} height={P} fill={c} opacity={op} />
  );

  return (
    <svg viewBox="0 0 200 300" className={className} style={{ width: '100%', height: '100%' }}>
      {/* ══ CUADRÍCULA DE FONDO ══ */}
      {Array.from({ length: 26 }).map((_, x) =>
        Array.from({ length: 38 }).map((_, y) => (
          <rect
            key={`g${x}-${y}`}
            x={x * P}
            y={y * P}
            width={P}
            height={P}
            fill="none"
            stroke={c1}
            strokeWidth="0.3"
            opacity={(x + y) % 2 === 0 ? 0.04 : 0.02}
          />
        ))
      )}

      {/* ══ CORAZÓN PIXELADO ══ */}
      {[
        [10, 4],
        [11, 4],
        [14, 4],
        [15, 4],
        [9, 5],
        [10, 5],
        [11, 5],
        [12, 5],
        [13, 5],
        [14, 5],
        [15, 5],
        [16, 5],
        [9, 6],
        [10, 6],
        [11, 6],
        [12, 6],
        [13, 6],
        [14, 6],
        [15, 6],
        [16, 6],
        [10, 7],
        [11, 7],
        [12, 7],
        [13, 7],
        [14, 7],
        [15, 7],
        [11, 8],
        [12, 8],
        [13, 8],
        [14, 8],
        [12, 9],
        [13, 9],
      ].map(([x, y], i) => (
        <g key={`heart${i}`}>
          {px(x, y, c1, 0.7)}
          {x === 10 && y === 5 && px(x, y, c4, 0.5)}
        </g>
      ))}
      {[
        [9, 4],
        [12, 4],
        [13, 4],
        [16, 4],
        [8, 5],
        [17, 5],
        [8, 6],
        [17, 6],
        [9, 7],
        [16, 7],
        [10, 8],
        [15, 8],
        [11, 9],
        [14, 9],
        [12, 10],
        [13, 10],
      ].map(([x, y], i) => (
        <g key={`hb${i}`}>{px(x, y, c2, 0.5)}</g>
      ))}

      {/* ══ ESPADA PIXELADA ══ */}
      {[
        [4, 14],
        [4, 15],
        [4, 16],
        [4, 17],
        [4, 18],
        [4, 19],
        [4, 20],
        [4, 21],
        [4, 22],
        [4, 23],
        [4, 24],
      ].map(([x, y], i) => (
        <g key={`blade${i}`}>
          {px(x, y, c4, 0.65)}
          {px(x + 1, y, c1, 0.55)}
        </g>
      ))}
      {px(4, 13, c5, 0.7)}
      {[2, 3, 4, 5, 6].map((x, i) => (
        <g key={`guard${i}`}>{px(x, 25, c2, 0.6)}</g>
      ))}
      {[26, 27, 28].map((y, i) => (
        <g key={`hilt${i}`}>{px(4, y, c3, 0.55)}</g>
      ))}
      {px(4, 29, c1, 0.5)}

      {/* ══ DIAMANTE / GEMA PIXELADA ══ */}
      {[
        [19, 14],
        [20, 14],
        [21, 14],
        [18, 15],
        [19, 15],
        [20, 15],
        [21, 15],
        [22, 15],
        [18, 16],
        [19, 16],
        [20, 16],
        [21, 16],
        [22, 16],
        [19, 17],
        [20, 17],
        [21, 17],
        [20, 18],
      ].map(([x, y], i) => (
        <g key={`gem${i}`}>{px(x, y, i < 3 ? c4 : i < 8 ? c1 : c2, 0.6)}</g>
      ))}
      {px(19, 15, c5, 0.5)}

      {/* ══ MONTAÑAS PIXELADAS ══ */}
      {[
        [3, 33],
        [4, 33],
        [5, 33],
        [6, 33],
        [7, 33],
        [8, 33],
        [9, 33],
        [4, 32],
        [5, 32],
        [6, 32],
        [7, 32],
        [8, 32],
        [5, 31],
        [6, 31],
        [7, 31],
        [6, 30],
      ].map(([x, y], i) => (
        <g key={`m1${i}`}>{px(x, y, c2, 0.45)}</g>
      ))}
      {px(6, 30, c5, 0.4)}
      {px(6, 31, c4, 0.3)}
      {[
        [14, 33],
        [15, 33],
        [16, 33],
        [17, 33],
        [18, 33],
        [19, 33],
        [20, 33],
        [21, 33],
        [15, 32],
        [16, 32],
        [17, 32],
        [18, 32],
        [19, 32],
        [20, 32],
        [16, 31],
        [17, 31],
        [18, 31],
        [19, 31],
        [17, 30],
        [18, 30],
        [17, 29],
      ].map(([x, y], i) => (
        <g key={`m2${i}`}>{px(x, y, c1, 0.4)}</g>
      ))}
      {px(17, 29, c5, 0.45)}
      {px(17, 30, c4, 0.35)}
      {px(18, 30, c4, 0.3)}

      {/* ══ SOL PIXELADO ══ */}
      {[
        [20, 23],
        [21, 23],
        [20, 24],
        [21, 24],
      ].map(([x, y], i) => (
        <g key={`sun${i}`}>{px(x, y, c4, 0.65)}</g>
      ))}
      {[
        [20, 21],
        [21, 21],
        [20, 26],
        [21, 26],
        [18, 23],
        [18, 24],
        [23, 23],
        [23, 24],
        [19, 22],
        [22, 22],
        [19, 25],
        [22, 25],
      ].map(([x, y], i) => (
        <g key={`ray${i}`}>{px(x, y, c1, 0.35)}</g>
      ))}

      {/* ══ NUBE PIXELADA ══ */}
      {[
        [2, 21],
        [3, 21],
        [4, 21],
        [5, 21],
        [1, 22],
        [2, 22],
        [3, 22],
        [4, 22],
        [5, 22],
        [6, 22],
        [2, 23],
        [3, 23],
        [4, 23],
        [5, 23],
      ].map(([x, y], i) => (
        <g key={`cloud${i}`}>{px(x, y, c5, 0.35)}</g>
      ))}

      {/* ══ ÁRBOL PIXELADO ══ */}
      {[
        [11, 28],
        [12, 28],
        [13, 28],
        [10, 29],
        [11, 29],
        [12, 29],
        [13, 29],
        [14, 29],
        [10, 30],
        [11, 30],
        [12, 30],
        [13, 30],
        [14, 30],
        [11, 31],
        [12, 31],
        [13, 31],
      ].map(([x, y], i) => (
        <g key={`tree${i}`}>{px(x, y, c1, 0.5)}</g>
      ))}
      {[32, 33].map((y, i) => (
        <g key={`trunk${i}`}>{px(12, y, c3, 0.5)}</g>
      ))}

      {/* ══ BARRAS DE VIDA / UI ══ */}
      {Array.from({ length: 10 }).map((_, i) => (
        <g key={`hpf${i}`}>
          {px(8 + i, 35, c2, 0.35)}
          {px(8 + i, 37, c2, 0.35)}
        </g>
      ))}
      {px(7, 36, c2, 0.35)}
      {px(18, 36, c2, 0.35)}
      {Array.from({ length: 8 }).map((_, i) => (
        <g key={`hp${i}`}>{px(8 + i, 36, i < 6 ? c1 : c4, 0.55)}</g>
      ))}

      {/* ══ MONEDA PIXELADA ══ */}
      {[
        [2, 35],
        [3, 35],
        [1, 36],
        [2, 36],
        [3, 36],
        [4, 36],
        [2, 37],
        [3, 37],
      ].map(([x, y], i) => (
        <g key={`coin${i}`}>{px(x, y, c4, 0.55)}</g>
      ))}
      {px(2, 36, c2, 0.4)}
      {px(3, 36, c2, 0.4)}

      {/* ══ ESTRELLA PIXELADA ══ */}
      {[[22, 4], [21, 5], [22, 5], [23, 5], [22, 6]].map(([x, y], i) => (
        <g key={`star${i}`}>{px(x, y, c4, 0.6)}</g>
      ))}

      {/* ══ CURSOR / FLECHA ══ */}
      {[
        [21, 9],
        [21, 10],
        [22, 10],
        [21, 11],
        [22, 11],
        [23, 11],
        [21, 12],
        [22, 12],
        [21, 13],
      ].map(([x, y], i) => (
        <g key={`cur${i}`}>{px(x, y, c5, 0.55)}</g>
      ))}
      {[
        [20, 9],
        [22, 9],
        [20, 10],
        [23, 10],
        [20, 11],
        [24, 11],
        [20, 12],
        [23, 12],
        [20, 13],
        [22, 13],
        [21, 14],
      ].map(([x, y], i) => (
        <g key={`curb${i}`}>{px(x, y, c3, 0.4)}</g>
      ))}

      {/* ══ BLOQUES DECORATIVOS SUELTOS ══ */}
      {[
        [1, 8, c1, 0.3],
        [2, 9, c2, 0.25],
        [0, 12, c1, 0.2],
        [23, 28, c2, 0.3],
        [24, 30, c1, 0.25],
        [22, 32, c3, 0.2],
        [13, 20, c1, 0.25],
        [14, 21, c2, 0.2],
        [8, 25, c4, 0.2],
        [9, 26, c1, 0.15],
        [0, 28, c2, 0.2],
        [1, 30, c1, 0.18],
        [24, 14, c3, 0.22],
        [23, 16, c1, 0.18],
        [10, 13, c2, 0.2],
        [15, 12, c1, 0.18],
      ].map(([x, y, c, op], i) => (
        <g key={`deco${i}`}>{px(x as number, y as number, c as string, op as number)}</g>
      ))}

      {/* ══ PATRÓN DITHER — textura retro ══ */}
      {Array.from({ length: 6 }).map((_, i) => (
        <g key={`dith${i}`}>
          {px(1 + i * 4, 2, c1, 0.08)}
          {px(3 + i * 4, 3, c1, 0.08)}
        </g>
      ))}

      {/* ══ POWER-UP BRILLANTE ══ */}
      {px(12, 15, c5, 0.7)}
      {px(12, 16, c4, 0.55)}
      {px(11, 16, c1, 0.35)}
      {px(13, 16, c1, 0.35)}
      {px(12, 17, c1, 0.35)}
      {px(11, 15, c4, 0.25)}
      {px(13, 15, c4, 0.25)}
      {px(12, 14, c4, 0.2)}
    </svg>
  );
}
